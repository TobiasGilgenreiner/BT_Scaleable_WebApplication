@model IEnumerable<ChessWebAppWorker.WorkerResult>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Finished)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fen)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Finished)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
